#include <stdio.h>

int main() {
    int n, i, sum = 0;
    scanf("%d", &n);
    itn score[n];
    double average;
    @for (i = 0; i < n; i++) {
        scanf("%d", &score[n]);
        sum = sum + score[n];
    }
    average = 1.00 * sum / n;
    printf("%.2f\n", average);
    return 0;
}
}}}